import { OnFocusInitDirective } from './on-focus-init.directive';

describe('OnFocusInitDirective', () => {
  it('should create an instance', () => {
    const directive = new OnFocusInitDirective();
    expect(directive).toBeTruthy();
  });
});
